# public/

- **`resume.pdf`** — the résumé every "Resume" button links to. Replace the file
  to update it; the path never changes.
- **`headshot_full.png`** — the hero portrait.
- **`sprite/`** — the walking character's frames. **Not yet added** — see `sprite/README.md`.

## Adding the character

The seven frames go in **`public/sprite/`**, which has its own README with the
filenames, the walk cycles, and what to do about the transparent margins.
